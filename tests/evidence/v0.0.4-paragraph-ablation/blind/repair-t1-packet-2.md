# T1 匿名复测包 2

任务和正文见 `../tasks.md` 与 `repair-t1-packet.md`，按以下关系重标：A = 匿名包 1 的 C，B = 匿名包 1 的 A，C = 匿名包 1 的 B。原标签只用于定位正文，不表示身份。

A、B、C 身份未知，不得猜测。先检查事实、数字、样本称谓、范围、因果和禁止补造项；任一硬错误记 FAIL。随后比较论证推进、证据解释、自然成段、重复和直接使用成本。篇幅只作为 Prompt 遵循项。
