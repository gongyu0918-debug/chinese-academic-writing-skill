# A/B 第一整轮技术结论

## 结论

第一整轮保存于 `ab-run1-invalid/`，共完成 24 次独立写稿调用，运行绑定稳定、模型重试数为 0。原始 manifest 判定 22/24 技术有效，因此本轮整体无效，不生成匿名包、不进入内容盲评，也不计入候选胜负。

## 两项无效记录

- `H1-Q3 / baseline / MiniMax`：返回码为 0、未超时、prompt 绑定正确，但 trace 出现两个 `agent_message`，违反“一次会话只接收一个首个 final”的预注册门禁。这是模型本次调用的真实违约。
- `H3-Q3 / baseline / MiniMax`：实际执行三次独立 `Get-Content -Raw -LiteralPath`，路径、返回码和读取内容均正确，但 PowerShell 包装命令在 `-Command` 前加入安全进程参数 `-NoProfile`。原校验器只接受无进程参数的等价命令，因此误判三份必读缺失。

## 校验器修正

只扩展精确读取命令的进程参数白名单，允许 `-NoProfile`、`-NonInteractive`、`-NoLogo` 以任意组合出现在 `-Command` 前。仍要求命令从头到尾匹配，只能执行 `Get-Content -Raw -LiteralPath '<精确必读路径>'`；复合命令、编码命令、其他参数、额外命令、非零读取、空输出、内容不匹配、跨工作树和畸形 trace 继续拒绝。

该修正只纠正命令包装的等价形式，不替换或重跑第一轮任何单项产物。由于 `H1-Q3` 的双消息违约仍成立，第一轮在修正后仍不具备合并资格。后续按原预注册保留本轮证据，另起完整目录，以相同任务、prompt、模型、effort、超时、映射和零重试规则重新运行全部 24 次。
