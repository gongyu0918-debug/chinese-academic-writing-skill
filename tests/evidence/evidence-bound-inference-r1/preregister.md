# 证据内推断 R1 预注册

## 目标与冻结范围

固定基线为 `main@dddda05f044f0402082cb0cd8ff567e68d9238c5`。第一原子只判断普通论文专项叶是否需要明确区分“作者基于材料的推断”与“新增经验事实”；不修改开题、独立综述、来源检索、ANTI-AI、脚本、版本或发布元数据。

先运行基线。只有至少两个 provider 在正向任务中共同出现以下任一目标缺口，才建立候选：只复述材料而不形成讨论；以连续兜底替代论证；把范围受控的作者解释判成事实外扩；对已经合格的推断强索与当前判断无关的次级材料。基线若没有共性缺口，本原子直接取消，不为理论完整性增加规则。

## 真实写稿矩阵

- 五题：讨论起草、底稿修改、混合审稿、相关性反控、合格审稿反控。
- 五条 provider 路线，均使用 `max` 思考、全新隔离目录、零质量重试；沿用中文公文写作项目已经用于真实写稿的低成本矩阵：
  - `alibaba-token-plan-2/deepseek-v4-flash-0731`
  - `alibaba-token-plan/deepseek-v4-flash-0731`
  - `ollama-cloud/deepseek-v4-flash:0731`
  - `opencode-go/deepseek-v4-flash`
  - `minimax-cn/MiniMax-M3`
- 每次只复制当前臂的运行 Skill；要求完整读取 `SKILL.md` 与 `references/academic-writing.md`，不得读取 Git、证据目录、其他 Skill 或联网，不修改文件。
- 基线与候选使用逐字相同的任务、外层提示和模型参数。原始 final、trace、stderr 和 manifest 只保存在忽略目录 `.release/evidence-bound-inference-r1/`。

## 判定

先逐稿判事实、来源、研究状态、因果与范围、输出模式，再比较直接可用性和修改负担。基于已给事实形成的范围受控归纳、比较、解释、候选原因或条件性意义不是新增事实；只有冒充来源内容、研究结果、已验证机制、确定因果、显著效果、已发生状态或范围外总体结论时才判外扩。

首轮不把与 DIFF 无关的模型波动、Markdown 偏好、措辞差异、技术失败或基线已有问题计作候选回退。发现候选独有硬失败时，先用全新任务确认是否可归因；成立则拆最小修正复测，不直接放弃整个方向。

候选进入工程门须同时满足：

1. 五题五路均获得可核验的基线/候选配对，技术无效格用全新同类任务补样，不做质量重抽。
2. 候选无经确认的事实、来源、研究状态、因果、范围或交付模式硬回退。
3. 正向收益至少跨两个任务、两个 provider 复现；盲审中 Candidate 胜数多于 Baseline，且有决定的配对中 Candidate 胜率高于 50%。
4. 两道反控不因放开推断而增加确定因果、唯一归因、总体外推或过度审稿。

不交付 `HOLD`。样本充分后，本原子只作“合并”或“取消”；真实写稿通过前不补产品胶水、机械门或发布改动。
