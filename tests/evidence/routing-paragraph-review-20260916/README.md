# 2026-09-16 路由、逐段写作与独立复核

决策：**合并经过核验的路由、逐段交付、独立复核与 Python 维护边界修改；取消两版否定提示候选。** 本轮不是版本发布，产品版本与已交付 WorkBuddy ZIP 仍为 0.1.4。

基线 `46e7532c43c7f27a9a3a2f2d66bc37907064c428`；独立工作树分支 `codex/routing-paragraph-review-20260916`。最终规则保存在 `snapshots/candidate-final-skill/`。规则、路线图、实际调用和评审意见分别归档，避免以一项证据代替另一项。

## 最值得看的证据

- [逐边路线审查](ROUTES.md)：三主叶四模式，离线引用、长稿、段落、原生复核和所有终止/回退出口。没有发现必然死循环、死锁或整条不可达叶；修补的是明确的连接缺口。
- [材料裁定与具体前后句](ADJUDICATION.md)：Qwen 独立复核发现段末“范围由此保持稳定，为下一步逐件比较提供了材料前提”重复且牵出后段，主写者删去并保留全部材料事实；也列出审稿模型本身的错误建议。
- [模型规范及精确标识](MODELS.md)：正式写稿五渠道、四模型家族，全部 `max`；Qwen Max、Kimi K3 冷审；早期 Astra/Luna 与 API high 不计正式验收。
- `continuation/`：两个便宜模型在原 session 继续下一段，两份首段哈希均未变；Qwen 只推进本段且未发现硬问题，MiniMax 添加未给的“与导师讨论”，单列问题。
- `delegation/`：9 个实际原生子会话的身份、父子关系、精确模型与可见意见，含续段产生的子会话。不是用 Python 生成评语，也不是把主写者自查称作独立复核。
- `audit-history/test-boundary-implementation.json`：53 项删除和 13 项拆分明细；历史原稿、三个运行脚本保持不变。

## 正式批次

所有批次均用原生 Codex CLI，精确请求、输入指纹、隔离目录与努力级别在各自 `native/<批次>/binding.json`。表中“技术有效”只指驱动取得正文及入口读取记录；**不是写作通过数，也不是辅助页加载通过数**。

| 批次 | 冻结比较 | 根调用数 | 技术有效 | 目的 |
|---|---|---:|---:|---|
| native-formal-initial | 0.1.4 / r2，五渠道 | 20 | 20 | 普通论文单段与否定冗余改稿 |
| native-formal-routes | 0.1.4 / r2，MiniMax | 20 | 19 | T1–T9、混合段落任务；基线 T1 未留下入口读取记录 |
| native-v2-review | 0.1.4 / r2，Qwen/MiniMax | 12 | 12 | V2 宿主的开题单段、改稿和多段范围 |
| native-v3-final | r2 / r3，Qwen/MiniMax | 12 | 12 | 冷审修正、提纲联审正文、结果与讨论复核 |
| native-v4-review-route | r3 / r4，Qwen/MiniMax | 8 | 7 | 审稿读取路径、正常结果段反控 |
| native-v4-technical-retry | 同 r4 的一次空响应重试 | 1 | 1 | 保留空响应，不为质量挑稿；重试仍跳过辅助页 |
| native-final-closure | r5，单臂两模型 | 6 | 6 | T9 引用与短交付、T10、缺原文审稿 |
| native-negation-boundary | r5 / r6 探针，两模型 | 8 | 8 | N2 冗余否定、必要双否定反控；r6 取消 |
| native-final-negation | 最终规则，单臂两模型 | 4 | 4 | 恢复旧否定段后的实际改稿、正常结果反控 |
| 原 session resume | r3 的已交首段继续，两模型 | 2 | 2 | 上段不变、当前研究方法段、保留拟开展状态 |

合计 91 次根 `exec` 调用（89 次技术有效）及 2 次 `exec resume`（2 次有效）。另有 7 次正式 `max` 冷审/匿名审阅，分别保存包、回复与绑定。不同阶段输入不同，不能合成候选总体胜率。首批基线少读入口、r4 MiniMax 空响应各保留一次，不静默剔除。

40 次 earlier API `high` 探索独立留在 `exploratory/`，仅解释候选形成过程，未用于替代正式模型规范。调用返回内容数不能当作独立实验家族数、质量通过数或完整计费量。

## 规则版本与取舍

- r2：最初路由、段落、宿主复核和否定简化候选。
- r3：补提纲联合正文的加载条件、来源/格式优先级、缺原文审稿，以及按结果与讨论职责复核；委派沿用授权。
- r4：入口显式要求审稿读取对应主叶与语言页。
- r5：统一跨轮检索授权、纯检索交付与提纲审查归属。
- r6：只在实验副本修改 ANTI-AI 第 1 步，区分材料事实与冗余说明；没有稳定净收益，取消。
- 最终：r5 结构，恢复 0.1.4 的“高频句式与词组”段落；保留独立复核顺序及结果段职责说明。两个否定文字候选都未合入。

竞争来源为固定 commit 的 `gov-doc-skills`、`humanizer-zh-tw`，以及实际只读下载的 SkillHub `govwriting`；来源哈希及版本冲突见 `audit-history/competitors-sep16.md` 和 `sources/`。只借可验证的段落职责，未引入公文结构、分值、禁词门或“每个观点只能说一次”。

## 维护验证

- `py -3 -B -m unittest discover -s tests -v`：**166/166 PASS**；整合后 9.112 秒。日志 `final-maintenance-tests.log`。较早拆分测试的 8.898 秒日志原样另存，不互相覆盖。
- `py -3 -B <skill-creator>/scripts/quick_validate.py chinese-academic-writing-assistant`：`Skill is valid!`，仅验证元数据格式，不证明语义质量。
- `git diff --check`：通过。
- 三个运行 Python 脚本与历史证据无修改；12 文件产品布局和 11 运行文件发布边界不变。没有新增 runtime 代理、Hook、语义 CI 或新的路由解析器。
- 两个历史 checker 只改说明与 CLI 帮助，继续保留可执行行为测试；它们的固定句词、计数与 PASS/FAIL 仅解释旧实验，不再管当前 Markdown 写作质量。

`drivers/` 是原有公文 CLI 驱动经过本任务参数适配后的证据副本，以内容哈希去重，不是产品模块。复现需在隔离工作树的 `.release/` 放置对应驱动，使用 `binding.json` 中的冻结输入、模型、case 和 effort，并重新核对当前可用 catalog；不直接从归档目录运行旧绝对路径。

归档删除隐藏 reasoning/analysis 项，保存转换前后哈希；无账户凭据。模型可见回复与请求按原样保留，主维护者的纠偏另写，不回改原始输出。原始模型输入可能含 CRLF，Git 的产品 Markdown 会规范化行尾；比较最终输入与产品时同时记录原始 SHA-256 和 LF 规范化 SHA-256，不能混称字节相等。

## 已知限制

MiniMax 两次只读入口就审稿，辅助页读取仍不稳定；多个模型会加路由或字数前言；独立审稿者也可能要求补材料外的原因、基线或机制。最终 N2 仍有外围否定及单一机构推断问题，未被包装成成功案例。优先用具体材料和位置核实意见，允许保持原文。

未发布新版本、未推送远端、未更新 WorkBuddy 包；未完成所有宿主实装及大规模跨学科长稿验证。此证据支持本轮限定维护修改，不支持“全部模型稳定提升”或“所有写作路径逐个实测通过”。
