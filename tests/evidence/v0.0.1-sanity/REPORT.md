# 0.0.1 真实写稿 sanity

候选提交 `bd20f464c15d2237df158dc90939c616b775e7d4` 使用 12 个核心任务，每个任务由两个相互独立的新鲜 writer 上下文生成一次，共保留 24 份原始输出。任务覆盖普通论文提纲、段落修改、只审不改、开题报告、独立叙述性综述、论文内综述路由，以及无数据统计、英文全文、AIGC 规避和通知误路由等边界场景。最终候选只在证据生成后将公开展示名改为“中文论文写作”，未改动写作契约；原始输出不做追溯性改写。

匿名化后，两名全新 verifier 只读取任务和匿名输出，分别检查硬失败、指令遵循、任务编排和风格。两名 verifier 共提交 48 条判定，全部为 `PASS`；硬失败、遵循失败、编排失败和风格警告均为 0。verifier 结果在解除匿名映射前封存并记录 SHA-256。

一次 L01 重试曾补出材料未支持的“研究对象不同”，该现象只在一个输出中出现。按照至少三次复现才修复的约定，仓库保留失败样本和首轮盲审记录，没有调整 Prompt、词表或清洗逻辑；最终有效样本恢复为事实边界正确的首次输出。

本轮模型 ID 无法取得，统一记录为 `unavailable`。这些结果只证明当前环境中的真实写稿 sanity，不构成跨模型统计结论。按用户最新要求，0.0.1 不再扩展基线、九宫格入口和四组材料消融，发布门只保留真实写稿、双盲复核、确定性检查和最小回归。
